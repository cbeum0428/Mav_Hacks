#include <iostream>
#include "NeuralNetwork.h"
#include <vector>

using namespace std;

int main() {
	
	vector<NeuralNetwork> pop;
	for (int i = 0;i < popoulationSize()
	
	NeuralNetwork* nn = new NeuralNetwork(topology);
	vector<int> topology;
	topology.push_back(931);
	//logic for building internal topology.
	topology.push_back(49);
	
	
	
}
